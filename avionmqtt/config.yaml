name: "avionmqtt"
slug: "avionmqtt"
description: "Avi-on / Halo Home BLE → MQTT bridge"
version: "0.1.0"

startup: services
boot: auto
init: false

arch:
  - amd64
  - aarch64
  - armv7
  - armhf

host_network: true

map:
  - type: homeassistant_config
    path: /config
    read_only: false

#options:
#  log_level: INFO

#schema:
#  log_level: list(DEBUG|INFO|WARNING|ERROR)
